version: '3.7'
services:
   sisk-app:
      container_name: 'sisk-app'
      build: ./
      ports:
         - '4200:4200'
      volumes:
         - '/app/node_modules'
         - '.:/app'
      stdin_open: true
